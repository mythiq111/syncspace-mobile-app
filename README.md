# SyncSpace Mobile App

Expo app that renders the frontend inside a WebView. Run the frontend first (`npm run dev` on :3000), then:

```bash
npm install
npm start      # scan QR with Expo Go, same Wi-Fi
```

Set `EXPO_PUBLIC_WEB_URL` to use a deployed site.
